html
    head
        meta(charset="UTF-8")
    body
        h1 안녕!!
            span=a+b
        //img(src="/public/img/img.png",style="width=100px")
        img(src="/img/img.png",style="width=100px")
        h2 미들웨어로 정적리소스 처리
            ul
                li app.use(express.static("경로"));
                li 확장자(.png, .jpeg, .hwp ..)가 정적 리소스인 경우 public  폴더에서 찾아서 응답
                li 응답하는 문서의 형식을 자동으로 처리한다.
        h2 express 에서 템플릿 엔진 적용 (pug)
            ul
                li app.set("view engine", "엔진이름");
                li express 사이트에 명시된 템플릿 엔진 응답으로 사용
                li app.set("views", "경로") default "./views" : 템플릿 엔진 파일들의 기본 경로
        h2 bodyParser 로 요청 헤더로 넘어오는 queryString 을 미들웨어로 처리
        h3 회원가입 폼 action=/singup.do
        //get : 파라미터에 포함
        //post : 요청헤더(요청에 대한 메타정보)에 쿼리스트링을 포함
        form(action="/signup.do" method="post")
            p
                label 아이디
                    input(name="id",value="hong123")
            p
                label 이름
                    input(name="name",value="홍길동")
            p
                label 직업
                    input(name="job",value="의적")
            p
                label 나이
                    input(name="age",value="???")
            p
                button(type="submit") 제출
        br
        br
        br
        h3 bodyParse 가 적용된 /signup2.do
        form(action="/signup2.do" method="post")
            p
                label 아이디
                    input(name="id",value="hong123")
            p
                label 이름
                    input(name="name",value="홍길동")
            p
                label 직업
                    input(name="job",value="의적")
            p
                label 나이
                    input(name="age",value="???")
            p
                button(type="submit") 제출
            br
            br
            br